---
name: code-flavor
description: 유저의 코드 작성 선호 사항. 절차적 또는 범용 프로그래밍 코드 작성/수정 시 확인
---

# 코드 작성 시

- 테스트 계약을 준수하는 내에서 최소 수정이 아닌 최소 코드 지향
  - 코드 중복을 줄이기 위해 넓은 범위 수정이 필요한 경우 일회성 해결 대신 제안
- 가변 상태 최소화
- 코드 매 일부분에서 입력, 결과를 명확하게 최소로 유지
- 저장소 규칙, 언어 제약, 컨벤션 내에서
  - 임포트 부수효과 선행, 공개 인터페이스·항목 → 구현 상세, 참조 → 피참조 순
    배치
- 코드 중복 서술 주석 대신 함수 추출 선호
- 배경·근거·데이터 보존 고려
  - 의도·해당 커밋에만 관련된 프롬프트, 기존 문제·시행착오·폐기 대안과
    이유·스쿼시 이력, 참고 URL, 원시 데이터·오류 메시지 등
  - 한 곳 이상 반영 고려: 코드·주석(의문 해소), 기존 유관 문서, 테스트
    이름(계약), 커밋 메시지(변경·역사)
  - 민감정보 제거·익명화, 로컬 경로는 재현성도 고려해 수정
- 데이터 흐름, 비즈니스 규칙(제약사항), 암묵지(바로 파악이 힘든 용어)를 가능한
  파악하고 의외이거나 작업 단축에 중요한 발견이 문서화되지 않았다면 문서화 제안
- 디노 컨벤션: 함수 인터페이스 설계 시 가능하다면 0-2개의 필수 인자와 필요 시
  마지막에 선택적 매개변수를 포함 가능한 plain object 내지 named parameter를
  받게 합니다
- 추측성 픽스 금지. 우회/완화는 한계, 제거 조건, 진단 수단 명시

# ponytail: 언급 또는 검토 시 아래 기준 적용

- 우선순위: 불필요하면 생략 → 기존 코드 재사용 → 표준 라이브러리 → 플랫폼 기능 →
  설치된 의존성 → 한 줄 → 최소 코드
- 버그는 모든 호출 경로 확인 후 공통 원인 한 곳에서 수정
- 합의 안 된 추상화·확장성·상용구·새 의존성 금지
- 삭제 > 추가, 평범함 > 영리함, 적은 파일 > 넓은 변경
- 최소 변경보다 올바른 위치·경계 조건 우선
- 복잡한 요청도 합리적 최소안부터 구현하고 생략 범위·확장 조건 명시
- 의도적 한계는 한계와 교체 조건을 주석으로 기록
- 비자명한 로직에는 가장 작은 실행 가능 검증 하나

# 일회성, 생성 코드, 설정이 아닌 프로덕션 코드 작성 시

tdd-flavor 확인
